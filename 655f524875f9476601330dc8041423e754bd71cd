BLE-Scan wird standardmaessig auf Bike-Hardware gefiltert: DongleService.startScan akzeptiert jetzt showAllDevices (Default false) und meldet nur Geraete, deren Name auf isBikeHardwareName passt (CONTROL, DMC, CONTROLDMC, FarDriver, YuanQ, FOC, BT, JDY, HM-10, ANT, BMS). connection_screen.dart hat einen Toggle 'Nur Bike-Hardware anzeigen' (Standard aktiv) / 'Alle Geräte anzeigen' fuer manuelle Suche; der Fusszeilen-Text spiegelt den Modus. BleTransport-Schnittstelle und AntBmsService sowie die Fake-Transports in den Tests wurden angepasst; EmptyState ist scroll-sicher gegen Overflow auf kleinen Viewports.

Verification: docker compose run --rm flutter dart format <geaenderte Dateien>; docker compose run --rm flutter flutter test -> 287 passed (inkl. 4 neuer Filter-Tests); flutter analyze -> 0 errors, nur vorbestehende infos/warnings in ant_bms_service.dart (unused _bmsServiceUuid/_bmsCharUuid/canWrite).
