Verification: docker compose run --rm flutter dart format lib/services/parameter_snapshot_repository.dart test/parameter_snapshot_repository_test.dart; docker compose run --rm flutter flutter test; docker compose run --rm flutter flutter analyze --no-fatal-infos. All tests passed; analyzer reports existing info-level lints only. T026 tests cover complete versus pending storage, raw-byte round-trip, and tamper detection.
